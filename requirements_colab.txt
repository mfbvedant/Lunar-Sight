# LunarSight — Google Colab Requirements
# ========================================
# Install in Colab with: !pip install -r requirements_colab.txt
#
# PyTorch and CUDA are pre-installed on Colab, so we skip them here.

# Core
numpy>=1.24,<2.0
scipy>=1.10
pyyaml>=6.0
requests>=2.28

# Geospatial (lightweight — GDAL is pre-installed on Colab)
rasterio>=1.3
pyproj>=3.4

# ML (SMP only — torch is pre-installed)
segmentation-models-pytorch>=0.3.3

# Orchestration
langgraph>=0.1

# Visualization
matplotlib>=3.7
Pillow>=9.5

# Utilities
tqdm>=4.65
